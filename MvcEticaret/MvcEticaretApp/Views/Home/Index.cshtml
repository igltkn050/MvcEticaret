@model List<Product>

@foreach (var item in Model)
{
    @await Component.InvokeAsync("Product", new { id = item.Id, title = item.Title, starCount = item.StarCount, oldPrice = item.OldPrice, price = item.Price, ImageUrl = item.ImageUrl, isSale = item.IsSale })
}